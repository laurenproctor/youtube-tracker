%table.table.table-striped.table-bordered.sortable
  %thead
    %tr
      %th= sortable DayVideoTracker, 'this_week_rank', 'This Week Rank'
      %th= sortable DayVideoTracker, 'last_week_rank', 'Last Week Rank'
      %th= sortable DayVideoTracker, 'name', 'Video Name'

      %th= sortable DayVideoTracker, 'weeks_on_chart', 'Weeks on Chart'
      %th= sortable DayVideoTracker, 'total_aggregate_views', 'Total Aggregate Views'
      %th= sortable DayVideoTracker, 'this_week_views', 'This Week Views'
      %th= sortable DayVideoTracker, 'weekly_percent_views', '% Change (Weekly Views)'
      %th= sortable DayVideoTracker, 'uploaded_at', 'Time Since Upload'
  %tbody
    - @top_videos.each do |p|
      %tr
        %td= p.this_week_rank
        %td= p.last_week_rank

        %td= p.name
        %td= pluralize(p.weeks_on_chart, 'week')
        %td= p.total_aggregate_views
        %td= p.this_week_views
        %td== #{ p.weekly_percent_views } %
        %td= time_ago_in_words p.uploaded_at if p.uploaded_at

.pagination-right

